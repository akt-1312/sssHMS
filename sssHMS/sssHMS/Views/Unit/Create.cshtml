@model sssHMS.Models.Unit
@{
    ViewData["Title"] = "Create";
    var title = "Unit";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>@title Create</h1>

<form method="post">
    <div asp-validation-summary="ModelOnly"></div>

    <input hidden name="returnUrl" value="@ViewBag.returnUrl" />
    
    <div class="row">
        @*<div class="form-group col-md-3">
                <label asp-for="Department" class="control-label"></label>
                <select asp-for="DepartmentID" class="form-control" asp-items="@(new SelectList(@ViewBag.DepList,"DepartmentID","DepartmentName"))"></select>

            </div>*@
        <div class="form-group col-md-3">
            <label asp-for="UnitName" class="control-label"></label>
            <input asp-for="UnitName" class="form-control" autofocus="autofocus" />
            <span asp-validation-for="UnitName" class="text-danger"></span>
        </div>
        
    </div>
    <div class="row">
        <div class="form-group col-md-5">
            <button type="submit" class="btn btn-success">Save</button>
            <a class="btn btn-primary" href="@ViewBag.returnUrl">Back To List</a>
        </div>
    </div>

</form>

@section Scripts{

    <script type="text/javascript">

    $(document).ready(function () {
        $('#text').click(function () {
            alert('ok');
            var c =@ViewBag.c;
            alert(c);
            var a = $('@ViewBag.c').prev().val();
            alert(a);

        })
    })
    </script>
}







